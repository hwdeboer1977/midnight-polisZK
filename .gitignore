# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Contract artifacts
contracts/managed/

# Deployment info
deployment.json

# OS generated files
.DS_Store
Thumbs.db

# IDE files
.vscode/settings.json
.idea/
*.swp
*.swo

# Logs
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory
coverage/

# Private state
*-state/

# Temporary files
tmp/
temp/

# Local probes/scratch
.scratch/

# Private state store (LevelDB)
midnight-level-db/

# Employer's salaries + nonces (private half of the payroll state).
# Not `payroll-secrets*.json`: that misses a backup like
# `payroll-secrets.json.bak`, which holds exactly the same salaries.
payroll-secrets*

# Deployment records kept around before a redeploy.
deployment.json.bak-*

# Cached wallet sync state
.wallet-state/

# Frontend build output and generated config
frontend/node_modules/
frontend/dist/
frontend/public/deployments.json

# Contract modules copied into the frontend by frontend:config
frontend/src/generated/

# Who has drawn a demo pEUR allowance — local bookkeeping, not source.
claims.json

# Compiled ZK assets copied for the browser prover (large build artifacts)
frontend/public/zk/
